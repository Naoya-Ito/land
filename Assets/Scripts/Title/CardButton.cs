using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CardButton : MonoBehaviour
{
    /*
  [SerializeField] Image CardArea;

  public void pressButton(){
    Canvas canvas = GameObject.Find("Canvas").GetComponent<Canvas>();
    Image panel = Instantiate(CardArea, canvas.transform);
    panel.name = "CardList";

    List<string> deck = new List<string>() {};
    foreach(string key in CardDrop.lv1_drop) {
      deck.Add(key);
    }
    foreach(string key in CardDrop.lv2_drop) {
      deck.Add(key);
    }
    foreach(string key in CardDrop.special_drop) {
      deck.Add(key);
    }
    CardList.instance.updateCardList(deck);
    CardList.instance.updateTitle("カード一覧");
  }
  */
}
